# AI/ML Portfolio (FastAPI + Jinja2)

## Setup
1. Create a virtualenv (recommended)
   ```bash
   python -m venv venv
   source venv/bin/activate    # mac/linux
   venv\Scripts\activate       # windows
